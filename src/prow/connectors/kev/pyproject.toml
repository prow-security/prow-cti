[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prow-connector-kev"
version = "0.1.0"
description = "CISA KEV connector for prow"
requires-python = ">=3.12"

[project.entry-points."prow.connectors"]
"cisa-kev" = "prow.connectors.kev:KevConnector"
