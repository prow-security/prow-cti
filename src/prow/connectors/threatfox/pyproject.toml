[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "prow-connector-threatfox"
version = "0.1.0"
description = "abuse.ch ThreatFox connector for prow"
requires-python = ">=3.12"

[project.entry-points."prow.connectors"]
"threatfox" = "prow.connectors.threatfox:ThreatFoxConnector"
